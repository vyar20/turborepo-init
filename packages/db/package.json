{
    "name": "@repo/db",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "db": "bun --env-file=../../apps/backend/.env prisma db push && bun --env-file=../../apps/backend/.env prisma generate"
    },
    "exports": {
        ".": "./src/db.ts"
    },
    "dependencies": {
        "@prisma/adapter-pg": "^7.8.0",
        "@prisma/client": "^7.8.0",
        "@repo/env": "workspace:*"
    },
    "devDependencies": {
        "@repo/config": "workspace:*",
        "prisma": "^7.8.0"
    }
}
