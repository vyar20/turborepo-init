{
    "name": "backend",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "NODE_ENV=development bun --env-file=.env --hot src/app.ts"
    },
    "dependencies": {},
    "devDependencies": {
        "@repo/config": "workspace:*"
    }
}
